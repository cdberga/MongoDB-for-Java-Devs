# iniciar o mongoshell
sudo service mongod start

# parar o mongoshell
sudo service mongod stop

# abrir o mongoshell
mongo

# MongoDB Relations:

=> One-to-one
Embed or not? Considerations:
- frequency of access
- size of items
- atomicity of data